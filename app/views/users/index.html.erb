<div class="card">
  <div class="card-header">
    <h4>Usuarios</h3>
  </div>
  <div class="card-body">
    <div class="row mb-3 mr-1 float-right">
      <a href="<%=new_user_path%>" class="btn btn btn-primary">
          <i class="material-icons md-18 align-middle">person_add</i>
          <span class="align-middle">Agregar</span>
        </a>
    </div>
    <div class="table-responsive">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th scope="col" class="text-left">Nombre</th>
            <th scope="col" class="text-left">email</th>
            <th/>
          </tr>
        </thead>
        <tbody>
          <%@users.each do |user|%>
            <tr>
              <td class="align-left text-left" scope="row"><%=user.full_name%></td>
              <td class="align-left text-left"><%=user.email%></td>
              <td class="align-left text-center">
                <a class="btn btn-white btn-sm" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" title="Acciones">
                  <i class="material-icons md-18 align-middle">more_vert</i>
                </a>

                <div class="dropdown-menu">
                  <%=link_to edit_user_path(user.id), title: 'Editar', class: "dropdown-item" do%>
                    <i class="material-icons md-14 align-middle">edit</i>
                    <span class="align-middle">Editar</span>
                  <%end%>

                  <div class="dropdown-divider"></div>

                  <%=link_to user_path(user.id), title: 'Borrar', class: 'dropdown-item text-danger', data: {confirm: 'Esta seguro?', method: :delete} do%>
                    <i class="material-icons md-14 align-middle">delete</i>
                    <span class="align-middle">Borrar</span>
                  <%end%>
                </div>
              </td>
            </tr>
          <%end%>
        </tbody>
      </table>
    </div>
  </div>
</div>